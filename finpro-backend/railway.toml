# railway.toml — Configuración para Railway.app
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
rootDir = "finpro-backend"  

[[services]]
name = "finpro-api"